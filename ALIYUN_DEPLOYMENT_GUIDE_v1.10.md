# 阿里云部署完整指南 - v1.10（小白版）

## 📖 说明

本指南专为0基础用户设计，每一步都有详细说明和截图提示。

**部署后你会得到什么？**
- ✅ 一个可以通过互联网访问的网页
- ✅ 功能完全一样：上传Excel文件 → 点击执行分析 → 查看报告
- ✅ 你的同事可以通过网址访问使用
- ✅ 24小时运行，不需要你的电脑开着
- ✅ **v1.10新特性**：ECharts交互式图表，更好的用户体验

---

## 🎯 部署流程概览

```
1. 购买阿里云服务器（约5分钟）
   ↓
2. 配置防火墙（约2分钟）
   ↓
3. 连接服务器（约2分钟）
   ↓
4. 安装环境（约10分钟）
   ↓
5. 部署代码（约5分钟）
   ↓
6. 配置运行（约5分钟）
   ↓
7. 完成！可以访问了
```

**总时间**：约30-40分钟

---

## 📋 第一部分：购买和配置服务器

### 步骤1：注册/登录阿里云账号

1. **访问阿里云官网**
   - 打开浏览器，访问：https://www.aliyun.com
   - 如果已有账号，点击右上角"登录"
   - 如果没有账号，点击"免费注册"

2. **注册账号**（如果没有）
   - 填写手机号、设置密码
   - 完成手机验证
   - 完成实名认证（需要身份证）

3. **登录账号**
   - 使用手机号或邮箱登录

### 步骤2：购买轻量应用服务器

1. **进入控制台**
   - 登录后，点击右上角"控制台"
   - 或访问：https://ecs.console.aliyun.com

2. **找到轻量应用服务器**
   - 在控制台首页，找到"轻量应用服务器"（Lightweight Application Server）
   - 或直接访问：https://swas.console.aliyun.com

3. **创建服务器**
   - 点击"创建服务器"或"立即购买"按钮
   - 进入购买页面

4. **选择配置**（重要！）
   
   **地域选择**：
   - 选择离你最近的地域，例如：
     - 华东1（杭州）- 适合华东地区
     - 华北2（北京）- 适合华北地区
     - 华南1（深圳）- 适合华南地区
   - **建议**：选择"华东1（杭州）"，速度较快
   
   **镜像选择**：
   - 选择"应用镜像"或"系统镜像"
   - **推荐**：选择"系统镜像" → "Ubuntu 22.04 64位"
   - 这是Linux系统，适合运行Python应用
   
   **套餐选择**：
   - **推荐**：选择"2核2G"或"2核4G"
   - 2核2G：约24-30元/月，适合小团队使用
   - 2核4G：约40-50元/月，性能更好
   - **建议**：先选择2核2G，如果不够用可以升级
   
   **购买时长**：
   - 选择1个月、3个月、6个月或1年
   - 时长越长，单价越便宜
   - **建议**：先选择1个月试用

5. **设置密码**
   - 设置root用户密码（这是服务器的管理员密码）
   - **重要**：一定要记住这个密码！建议保存到安全的地方
   - 密码要求：至少8位，包含大小写字母和数字

6. **确认购买**
   - 检查配置和价格
   - 勾选"服务协议"
   - 点击"立即购买"或"确认订单"
   - 完成支付

7. **等待服务器创建**
   - 购买成功后，等待1-3分钟
   - 服务器会自动创建并启动

### 步骤3：记录服务器信息

1. **查看服务器信息**
   - 在轻量应用服务器控制台，找到你刚购买的服务器
   - 点击服务器名称进入详情页

2. **记录以下信息**（非常重要！）
   
   在纸上或文档中记录：
   ```
   服务器公网IP：47.xxx.xxx.xxx（替换为实际IP）
   用户名：root
   密码：你设置的密码
   ```
   
   **如何查看IP**：
   - 在服务器详情页，找到"公网IP"或"IP地址"
   - 复制这个IP地址

### 步骤4：配置防火墙（安全组）

**什么是防火墙？**
- 防火墙控制哪些端口可以访问
- 需要开放端口，才能从外部访问你的应用

1. **进入防火墙设置**
   - 在服务器详情页，找到"防火墙"标签
   - 点击"防火墙"标签

2. **添加规则**
   - 点击"添加规则"按钮
   - 会弹出添加规则的对话框

3. **添加HTTP访问规则**
   - **应用类型**：选择"自定义"
   - **协议**：选择"TCP"
   - **端口范围**：输入`80`
   - **授权对象**：输入`0.0.0.0/0`（表示允许所有IP访问）
   - **备注**：输入`HTTP访问`
   - 点击"确定"

4. **添加HTTPS访问规则**（可选，用于SSL）
   - 点击"添加规则"
   - **协议**：TCP
   - **端口范围**：`443`
   - **授权对象**：`0.0.0.0/0`
   - **备注**：`HTTPS访问`
   - 点击"确定"

5. **添加应用端口规则**（如果不用Nginx）
   - 点击"添加规则"
   - **协议**：TCP
   - **端口范围**：`5000`
   - **授权对象**：`0.0.0.0/0`
   - **备注**：`Flask应用`
   - 点击"确定"

6. **确认规则**
   - 应该看到3条规则：
     - 端口80（HTTP）
     - 端口443（HTTPS，可选）
     - 端口5000（应用，如果不用Nginx）

---

## 📋 第二部分：连接服务器

### 步骤5：在Mac上连接服务器

**什么是SSH？**
- SSH是一种远程连接服务器的方式
- 就像远程控制服务器一样

1. **打开终端**
   - 在Mac上，按`Command + 空格键`
   - 输入"终端"或"Terminal"
   - 按回车打开终端

2. **连接服务器**
   
   在终端中输入以下命令（**替换为你的实际IP**）：
   ```bash
   ssh root@你的服务器IP
   ```
   
   **例如**，如果你的IP是`47.123.45.67`，则输入：
   ```bash
   ssh root@47.123.45.67
   ```
   
   按回车执行

3. **首次连接确认**
   - 首次连接会提示：
     ```
     The authenticity of host '47.xxx.xxx.xxx' can't be established.
     Are you sure you want to continue connecting (yes/no)?
     ```
   - 输入`yes`，按回车

4. **输入密码**
   - 会提示：`root@47.xxx.xxx.xxx's password:`
   - **输入你设置的服务器密码**（输入时不会显示，这是正常的）
   - 输入完成后按回车

5. **连接成功**
   - 如果密码正确，你会看到类似这样的提示：
     ```
     Welcome to Ubuntu 22.04 LTS...
     
     root@iZxxx:~#
     ```
   - 看到`root@xxx:~#`就说明连接成功了！

**如果连接失败**：
- 检查IP地址是否正确
- 检查密码是否正确
- 检查服务器是否已启动（在阿里云控制台查看）

---

## 📋 第三部分：安装环境

### 步骤6：更新系统并安装基础软件

**现在你在服务器上了**，所有命令都在服务器上执行。

1. **更新系统**
   
   在终端中输入：
   ```bash
   apt update && apt upgrade -y
   ```
   
   按回车执行，等待完成（可能需要几分钟）
   
   **说明**：
   - `apt update`：更新软件包列表
   - `apt upgrade -y`：升级所有软件包
   - `-y`：自动确认，不需要手动输入yes

2. **安装Python和工具**
   
   输入以下命令：
   ```bash
   apt install -y python3 python3-pip python3-venv git nginx curl wget vim
   ```
   
   按回车执行，等待完成
   
   **说明**：
   - `python3`：Python 3解释器
   - `python3-pip`：Python包管理器
   - `python3-venv`：Python虚拟环境工具
   - `git`：代码版本控制工具
   - `nginx`：Web服务器（用于反向代理）
   - `curl`、`wget`：下载工具
   - `vim`：文本编辑器

3. **验证安装**
   
   输入以下命令检查：
   ```bash
   python3 --version
   pip3 --version
   git --version
   nginx -v
   ```
   
   应该都能显示版本号，说明安装成功

---

## 📋 第四部分：部署代码

### 步骤7：从GitHub获取代码

**有两种方式**：

#### 方式1：如果代码已在GitHub上（推荐）

1. **进入合适的目录**
   ```bash
   cd /opt
   ```

2. **克隆代码**
   
   输入以下命令（**替换为你的GitHub仓库地址**）：
   ```bash
   git clone https://github.com/你的用户名/review_analysis_v1.10.git
   ```
   
   **例如**，如果你的GitHub用户名是`vincent`，则输入：
   ```bash
   git clone https://github.com/vincent/review_analysis_v1.10.git
   ```
   
   **如果还没创建GitHub仓库**，先参考`GITHUB_DEPLOYMENT_GUIDE.md`创建仓库并推送代码。

3. **进入项目目录**
   ```bash
   cd review_analysis_v1.10
   ```

4. **重命名主应用文件**（v1.10特有）
   ```bash
   # 将app_v1.10.py复制为app.py（用于生产环境）
   cp app_v1.10.py app.py
   ```
   
   **说明**：为了方便部署，我们使用`app.py`作为生产环境的主应用文件

#### 方式2：直接上传文件（如果GitHub还没准备好）

**在本地Mac终端执行**（不是服务器）：

```bash
# 进入项目目录
cd /Users/Vincent/Desktop/Review_Analysis

# 上传文件到服务器（替换为你的服务器IP）
scp -r backend frontend app_v1.10.py requirements.txt Procfile root@你的服务器IP:/opt/review_analysis_v1.10/
```

然后在服务器上：
```bash
mkdir -p /opt/review_analysis_v1.10
cd /opt/review_analysis_v1.10
# 重命名主应用文件
cp app_v1.10.py app.py
```

### 步骤8：创建Python虚拟环境

**什么是虚拟环境？**
- 虚拟环境是独立的Python环境
- 可以避免不同项目的依赖冲突

1. **创建虚拟环境**
   ```bash
   cd /opt/review_analysis_v1.10
   python3 -m venv venv
   ```
   
   等待完成（可能需要几秒钟）

2. **激活虚拟环境**
   ```bash
   source venv/bin/activate
   ```
   
   **激活成功的标志**：命令行前面会显示`(venv)`
   
   例如：
   ```
   (venv) root@iZxxx:/opt/review_analysis_v1.10#
   ```

3. **升级pip**
   ```bash
   pip install --upgrade pip
   ```

### 步骤9：安装Python依赖

**v1.10新增依赖**：
- `pyecharts>=2.0.0`（ECharts图表库）

1. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```
   
   等待完成（可能需要5-10分钟，取决于网络速度）
   
   **说明**：这会安装所有需要的Python包，包括Flask、pandas、pyecharts等

2. **验证安装**
   ```bash
   python3 -c "import flask; print('Flask已安装')"
   python3 -c "import pandas; print('pandas已安装')"
   python3 -c "import pyecharts; print('pyecharts已安装')"
   ```
   
   如果都显示"已安装"，说明安装成功

---

## 📋 第五部分：配置生产环境

### 步骤10：测试应用是否能运行

1. **测试运行**
   ```bash
   # 确保在虚拟环境中（应该看到(venv)）
   source venv/bin/activate
   
   # 测试运行（使用app.py）
   python3 app.py
   ```
   
   应该看到启动信息，类似：
   ```
   ============================================================
   Shopee竞品评价分析系统 v1.10
   ============================================================
   服务器启动中...
   访问地址: http://localhost:5000
   ```

2. **停止测试**
   - 按`Ctrl + C`停止应用
   - 这只是测试，后面会用更好的方式运行

### 步骤11：创建systemd服务（保持应用运行）

**什么是systemd？**
- systemd是Linux系统的服务管理器
- 可以让应用在后台运行，服务器重启后自动启动

1. **创建服务文件**
   
   输入以下命令创建服务文件（**注意：使用app.py而不是app_v1.10.py**）：
   ```bash
   cat > /etc/systemd/system/review-analysis.service << 'EOF'
   [Unit]
   Description=Shopee Review Analysis System v1.10
   After=network.target
   
   [Service]
   Type=simple
   User=root
   WorkingDirectory=/opt/review_analysis_v1.10
   Environment="PATH=/opt/review_analysis_v1.10/venv/bin"
   ExecStart=/opt/review_analysis_v1.10/venv/bin/gunicorn -w 2 -b 127.0.0.1:5000 --timeout 120 app:app
   Restart=always
   RestartSec=10
   
   [Install]
   WantedBy=multi-user.target
   EOF
   ```
   
   **说明**：
   - 这个文件告诉系统如何启动你的应用
   - 使用gunicorn作为Web服务器（比Flask自带的更稳定）
   - 应用会在127.0.0.1:5000上运行（只能本机访问）
   - **重要**：`app:app`表示使用`app.py`文件中的`app`对象

2. **重新加载systemd**
   ```bash
   systemctl daemon-reload
   ```

3. **启动服务**
   ```bash
   systemctl start review-analysis
   ```

4. **设置开机自启**
   ```bash
   systemctl enable review-analysis
   ```
   
   这样服务器重启后，应用会自动启动

5. **查看服务状态**
   ```bash
   systemctl status review-analysis
   ```
   
   **应该看到**：
   - `Active: active (running)` - 说明服务正在运行
   - 绿色的"active"状态
   
   如果看到错误，可以查看日志：
   ```bash
   journalctl -u review-analysis -f
   ```

6. **测试应用是否运行**
   ```bash
   # 检查端口是否监听
   netstat -tlnp | grep 5000
   ```
   
   应该看到类似：`tcp 0 0 127.0.0.1:5000`，说明应用正在运行

### 步骤12：配置Nginx反向代理（推荐）

**为什么需要Nginx？**
- Nginx是专业的Web服务器，性能更好
- 可以处理静态文件
- 可以配置HTTPS
- 更安全（应用只监听本地）

1. **创建Nginx配置文件**
   
   **重要**：将下面的`你的服务器IP`替换为你的实际IP地址！
   
   ```bash
   # 先获取你的服务器IP（在服务器上执行）
   curl ifconfig.me
   ```
   
   复制显示的IP地址，然后执行：
   ```bash
   cat > /etc/nginx/sites-available/review-analysis << EOF
   server {
       listen 80;
       server_name $(curl -s ifconfig.me);
   
       client_max_body_size 100M;
   
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host \$host;
           proxy_set_header X-Real-IP \$remote_addr;
           proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto \$scheme;
           
           proxy_connect_timeout 300s;
           proxy_send_timeout 300s;
           proxy_read_timeout 300s;
       }
   }
   EOF
   ```
   
   **或者手动创建**（更安全）：
   ```bash
   vim /etc/nginx/sites-available/review-analysis
   ```
   
   按`i`进入编辑模式，粘贴以下内容（**替换IP**）：
   ```nginx
   server {
       listen 80;
       server_name 你的服务器IP;  # 替换为你的实际IP，例如：47.123.45.67
   
       client_max_body_size 100M;
   
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
           
           proxy_connect_timeout 300s;
           proxy_send_timeout 300s;
           proxy_read_timeout 300s;
       }
   }
   ```
   
   按`Esc`，输入`:wq`，按回车保存

2. **启用配置**
   ```bash
   # 创建符号链接
   ln -s /etc/nginx/sites-available/review-analysis /etc/nginx/sites-enabled/
   
   # 删除默认配置（避免冲突）
   rm -f /etc/nginx/sites-enabled/default
   ```

3. **测试Nginx配置**
   ```bash
   nginx -t
   ```
   
   **应该看到**：
   ```
   nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
   nginx: configuration file /etc/nginx/nginx.conf test is successful
   ```
   
   如果看到错误，检查配置文件中的IP是否正确

4. **重启Nginx**
   ```bash
   systemctl restart nginx
   ```

5. **设置Nginx开机自启**
   ```bash
   systemctl enable nginx
   ```

6. **检查Nginx状态**
   ```bash
   systemctl status nginx
   ```
   
   应该看到`Active: active (running)`

---

## 📋 第六部分：验证部署

### 步骤13：检查所有服务

1. **检查应用服务**
   ```bash
   systemctl status review-analysis
   ```
   
   应该看到`active (running)`

2. **检查Nginx服务**
   ```bash
   systemctl status nginx
   ```
   
   应该看到`active (running)`

3. **检查端口**
   ```bash
   netstat -tlnp | grep -E "5000|80"
   ```
   
   应该看到：
   - `tcp 0 0 127.0.0.1:5000` - Flask应用
   - `tcp 0 0 0.0.0.0:80` - Nginx

### 步骤14：访问应用

1. **在浏览器中打开**
   - 打开浏览器（Chrome、Safari等）
   - 在地址栏输入：`http://你的服务器IP`
   - **例如**：`http://47.123.45.67`
   - 按回车

2. **应该看到**
   - 文件上传界面
   - 左侧有"文件上传"区域
   - 右侧有"分析报告"区域
   - **v1.10新特性**：所有图表都是交互式的（鼠标悬停会显示数据）

3. **如果看不到**
   - 检查防火墙是否开放了80端口
   - 检查服务是否运行：`systemctl status review-analysis`
   - 查看日志：`journalctl -u review-analysis -f`

### 步骤15：测试功能

1. **准备测试文件**
   - 使用项目中的测试文件：`reviews/`目录中的Excel文件
   - 或使用你自己的Excel文件

2. **上传文件**
   - 在网页上点击"选择文件"或拖拽文件
   - 选择Excel文件

3. **执行分析**
   - 点击"执行分析"按钮
   - 等待处理（通常几秒钟）

4. **查看报告**
   - 右侧应该显示完整的分析报告
   - 包含所有图表和数据
   - **v1.10新特性**：所有图表都支持交互（可以缩放、悬停查看数据等）

5. **测试完成**
   - 如果能看到报告，说明部署成功！

---

## 🎉 部署完成！

### 你现在拥有：

✅ **一个可以通过互联网访问的网页**
- 网址：`http://你的服务器IP`
- 功能完全一样：上传Excel → 分析 → 查看报告
- **v1.10新特性**：ECharts交互式图表，更好的用户体验

✅ **24小时运行**
- 服务器一直运行
- 不需要你的电脑开着
- 同事随时可以访问

✅ **稳定可靠**
- 使用专业的Web服务器（Gunicorn + Nginx）
- 服务器重启后自动启动

### 分享给同事：

告诉你的同事访问：
```
http://你的服务器IP
```

例如：
```
http://47.123.45.67
```

---

## 🔧 常用维护命令

### 查看日志

```bash
# 查看应用日志
journalctl -u review-analysis -f

# 查看Nginx访问日志
tail -f /var/log/nginx/access.log

# 查看Nginx错误日志
tail -f /var/log/nginx/error.log
```

### 重启服务

```bash
# 重启应用
systemctl restart review-analysis

# 重启Nginx
systemctl restart nginx
```

### 更新代码

```bash
# 进入项目目录
cd /opt/review_analysis_v1.10

# 如果使用Git，拉取最新代码
git pull origin main

# 激活虚拟环境
source venv/bin/activate

# 更新依赖（如果有新依赖）
pip install -r requirements.txt

# 如果app.py文件有变化，需要重启服务
systemctl restart review-analysis
```

---

## ⚠️ 常见问题解决

### 问题1：无法连接服务器

**症状**：`ssh root@IP`连接失败

**解决**：
1. 检查IP地址是否正确
2. 检查服务器是否运行（在阿里云控制台查看）
3. 检查密码是否正确
4. 检查安全组是否开放了SSH端口（22）

### 问题2：应用无法访问

**症状**：浏览器显示"无法访问此网站"

**检查步骤**：
```bash
# 1. 检查服务是否运行
systemctl status review-analysis

# 2. 检查Nginx是否运行
systemctl status nginx

# 3. 检查端口
netstat -tlnp | grep -E "5000|80"

# 4. 检查防火墙
# 在阿里云控制台检查安全组规则
```

**解决**：
- 确保服务正在运行
- 确保防火墙开放了80端口
- 检查Nginx配置是否正确

### 问题3：文件上传失败

**症状**：上传文件时报错

**检查**：
```bash
# 检查Nginx配置中的文件大小限制
cat /etc/nginx/sites-available/review-analysis | grep client_max_body_size
```

**解决**：
- 确保`client_max_body_size 100M;`在Nginx配置中
- 确保Flask的`MAX_CONTENT_LENGTH`也是100MB

### 问题4：图表不显示（v1.10特有）

**症状**：图表区域空白，看不到图表

**可能原因**：
- ECharts.js CDN无法加载（网络问题）

**检查**：
```bash
# 检查前端文件是否正确
cat /opt/review_analysis_v1.10/frontend/index.html | grep echarts
```

**应该看到**：
```html
<script src="https://cdn.jsdelivr.net/npm/echarts@5.4.3/dist/echarts.min.js"></script>
```

**解决**：
- 如果CDN无法访问，可以下载ECharts.js到本地
- 或使用其他CDN源（如unpkg.com）

### 问题5：内存不足

**症状**：处理大文件时服务器卡死

**解决**：
- 升级服务器配置（在阿里云控制台升级）
- 或限制文件大小

---

## 📞 需要帮助？

如果遇到问题：

1. **查看日志**：`journalctl -u review-analysis -f`
2. **检查服务状态**：`systemctl status review-analysis`
3. **查看本指南**：重新检查相关步骤
4. **联系阿里云技术支持**：在阿里云控制台提交工单

---

## ✅ 部署检查清单

完成以下检查项：

- [ ] 阿里云账号已注册/登录
- [ ] 服务器已购买
- [ ] 服务器信息已记录（IP、用户名、密码）
- [ ] 防火墙已配置（开放80、443、5000端口）
- [ ] 服务器已连接（SSH）
- [ ] 系统已更新
- [ ] Python和工具已安装
- [ ] 代码已部署到服务器
- [ ] app_v1.10.py已复制为app.py
- [ ] 虚拟环境已创建
- [ ] 依赖已安装（包括pyecharts）
- [ ] systemd服务已创建（使用app:app）
- [ ] 应用服务已启动
- [ ] Nginx已配置
- [ ] Nginx已启动
- [ ] 应用可以访问（浏览器打开IP）
- [ ] 功能测试通过（上传文件、生成报告、图表交互）

---

## 🆕 v1.10版本特有说明

### 相比v1.9的主要变化：

1. **新增依赖**：`pyecharts>=2.0.0`
2. **图表库切换**：从Matplotlib/Plotly切换到ECharts
3. **前端变化**：使用ECharts.js CDN
4. **主应用文件**：使用`app.py`（从app_v1.10.py复制）

### 部署时的注意事项：

1. **确保pyecharts已安装**：
   ```bash
   pip install pyecharts>=2.0.0
   ```

2. **确保app.py文件存在**：
   ```bash
   # 如果只有app_v1.10.py，需要复制为app.py
   cp app_v1.10.py app.py
   ```

3. **systemd服务配置**：
   - 使用`app:app`（不是app_v1.10:app）
   - 工作目录：`/opt/review_analysis_v1.10`

4. **前端ECharts.js加载**：
   - 前端会自动从CDN加载ECharts.js
   - 如果CDN无法访问，可能需要使用本地文件

---

**完成所有步骤后，你就有一个可以通过互联网访问的稳定网页了！** 🎉